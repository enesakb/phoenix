# Funding configuration is intentionally empty for now.
# The project will accept funding only after the Trail of Bits audit completes
# and the legal entity is registered. This file is committed so the GitHub
# "Sponsor" prompt does not appear with placeholder data.
